# Football King Web - Deployment Guide

1. Upload everything inside `FootballKing_Web/` to a GitHub repository.
2. Enable GitHub Pages from repo settings:
   - Source: `main` branch
   - Folder: `/root` or `/docs` depending on structure
3. Access the game via:
   https://<your-github-username>.github.io/<repo-name>/

